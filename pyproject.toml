[project]
name = "venex"
version = "0.1.0"
description = "Python backend API for fetching Venezuela currency exchange rates."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
]
